# Online Shop Web Page -- Alphashop
Alphashop is an online shopping web page built using React.

## Setup
1.Clone this repository
  ```
  git clone https://github.com/magic9701/alphashop.git
  ```
2.Install the required modules
  ```
  $ npm install
  ```
3.Start the website
  ```
  $ npm start
  ```